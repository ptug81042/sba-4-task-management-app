// Function to add a new task
function addTask(taskName, category, deadline, status) {
    const task = {
        name: taskName,
        category: category,
        deadline: deadline,
        status: status
    };
}